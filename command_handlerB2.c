#include "shell.h"

/**
 * string_tokenization - Tokenize a command into an array of strings
 * @command: The command to be tokenized
 *
 * Return: An array of strings
 */
char **string_tokenization(char *command)
{
	/* Implementation details */
}

/**
 * my_exit - Exit the shell
 * @argument: The arguments for the exit command
 *
 * Return: None
 */
void my_exit(char **argument)
{
/* Implementation details */
}

/**
 * check_build_in_func - Check if a command is a built-in function
 * @command: The command to be checked
 * @envp: An array of strings containing the environment variables
 *
 * Return: 1 if the command is a built-in function, 0 otherwise
 */
int check_build_in_func(char *command, char *envp[])
{
	/* Implementation details */
}

/**
 * file_check - Check if a command is a file
 * @command: The command to be checked
 *
 * Return: The file path if the command is a file, NULL otherwise
 */
char *file_check(char *command)
{
/* Implementation details */
}

